services:
  - type: web
    name: greencart-frontend
    env: static
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: frontend/build
    envVars:
      - key: REACT_APP_API_URL
        value: https://green-cart-pi-sepia.vercel.app/api
    routes:
      - type: rewrite
        source: /*
        destination: /index.html